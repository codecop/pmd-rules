<?xml version='1.0'?>
<project name="HBDAddon" default="run" basedir=".">

  <!-- set global properties for this build -->
  <property name="src" location="src"/>
  <property name="build" location="classes"/>
  <property name="lib"  location="."/>
  <property name="jarname" value="HBDAddon.jar"/>

  <target name="jar">
    <!-- make a jar file -->
    <jar jarfile="${lib}/${jarname}" basedir="${build}/">
    	<manifest>
    		<attribute name="Built-By" value="KoR"/>
    	</manifest>
    </jar>
  </target>

  <target name="run" depends="jar">
  </target>

</project>
